Had trouble installing sentence-transformers, overcome with:
	`pip install --no-deps sentence-transformers tqdm numpy scikit-learn scipy nltk transformers tokenizers requests`

Also need Python >= 3.6 and PyTorch (conda install torch) >= 1.6.0
